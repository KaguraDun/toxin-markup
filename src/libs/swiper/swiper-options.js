const swiperOptions = {
  pagination: {
    pagination: {
      el: '.swiper-pagination',
      clickable: true,
    },
  },
  navigation: {
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  },
  loop: { loop: true },
};

export default swiperOptions;
